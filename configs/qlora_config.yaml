model:
  model_id: "Qwen/Qwen2.5-7B-Instruct"
  trust_remote_code: true
  load_in_4bit: true
  bnb_4bit_quant_type: "nf4"
  bnb_4bit_compute_dtype: "bfloat16"   # T4/A100: float16, RTX 4090: bfloat16 
  bnb_4bit_use_double_quant: true

lora:
  r: 64
  lora_alpha: 128
  lora_dropout: 0.05
  bias: "none"
  task_type: "CAUSAL_LM"
  target_modules:
    - "q_proj"
    - "k_proj"
    - "v_proj"
    - "o_proj"
    - "gate_proj"
    - "up_proj"
    - "down_proj"

training:
  output_dir: "outputs/jinyong-qlora"
  per_device_train_batch_size: 4
  gradient_accumulation_steps: 4       # effective batch = 16
  learning_rate: 2.0e-4
  lr_scheduler_type: "cosine"
  warmup_ratio: 0.05
  num_train_epochs: 2
  # BUG FIX: was 512 — 300-char Chinese chunks format to ~651 tokens;
  # 512 silently truncated outputs during training.
  max_seq_length: 1024
  save_steps: 100
  eval_steps: 100
  save_total_limit: 3
  logging_steps: 10
  report_to: "none"
  fp16: false
  bf16: true
  # BUG FIX: was true — packing=True concatenates samples across chat
  # boundaries, teaching the model to ignore <|im_start|> tokens and
  # treat all input as raw continuation. This was the primary cause of
  # the model reproducing original Jin Yong text instead of following
  # the instruction.
  packing: false
  gradient_checkpointing: true         # saves ~3 GB VRAM, no accuracy loss
  eval_split_ratio: 0.05
  seed: 42

data:
  instruction_jsonl: "data/instructions/jinyong_sft.jsonl"
  raw_txt_dir: "data/raw"
  processed_txt_dir: "data/processed"
  system_prompt: "你是一位精通金庸武侠风格的写作助手。请根据用户的要求，创作符合金庸武侠小说风格的原创内容。文笔典雅，人物鲜明，江湖气息浓厚。不要复述原著情节，创作全新场景。"
